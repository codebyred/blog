[
{{- $pages := .Site.RegularPages -}}
{{- $len := len $pages -}}
{{- range $i, $page := $pages -}}
{
  "title": {{ $page.Title | jsonify }},
  "permalink": {{ $page.Permalink | jsonify }},
  "tags": {{ $page.Params.tags | default (slice) | jsonify }}
}{{ if lt (add $i 1 0) $len }},{{ end }}
{{- end -}}
]